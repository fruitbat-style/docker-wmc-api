services:
  db:
    image: postgres:17
    environment:
      POSTGRES_USER: wmc
      POSTGRES_PASSWORD: wmc_dev
      POSTGRES_DB: wmcdb
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
